# Input/Output
To pass data in or out of competitions (e.g. a registered players) you have to use the input and output nodes.
In the parent competition you can make connections to or from the competition like a normal node.

## Input 
You can have multiple input nodes per competition but all have the same value.

## Output
There can only be a maximum of one Output node per competition.  

