// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchSharedPreConfigArgs {
    pub match_id: u32,
    pub config_id: u32,
}

impl From<MatchSharedPreConfigArgs> for super::Reducer {
    fn from(args: MatchSharedPreConfigArgs) -> Self {
        Self::MatchSharedPreConfig {
            match_id: args.match_id,
            config_id: args.config_id,
        }
    }
}

impl __sdk::InModule for MatchSharedPreConfigArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_shared_pre_config`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_shared_pre_config {
    /// Request that the remote module invoke the reducer `match_shared_pre_config` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`match_shared_pre_config:match_shared_pre_config_then`] to run a callback after the reducer completes.
    fn match_shared_pre_config(&self, match_id: u32, config_id: u32) -> __sdk::Result<()> {
        self.match_shared_pre_config_then(match_id, config_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `match_shared_pre_config` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn match_shared_pre_config_then(
        &self,
        match_id: u32,
        config_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl match_shared_pre_config for super::RemoteReducers {
    fn match_shared_pre_config_then(
        &self,
        match_id: u32,
        config_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MatchSharedPreConfigArgs {
                match_id,
                config_id,
            },
            callback,
        )
    }
}
